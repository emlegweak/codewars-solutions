// split in parts

var splitInParts = function (s, partLength) {
    let splitParts = []
    for (let i = 0; i < s.length; i += partLength) {
        splitParts.push(s.slice(i, i + partLength))
    }
    return splitParts.join(' ')
}